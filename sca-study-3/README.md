# sca-study-3 — 高级应用模式

第四阶段：Function Calling / Tool Calling / Chat Memory / Vector Store。涵盖从旧版 `.functions()` 到新版 `@Tool` 注解的完整函数调用演变，内存对话记忆与会话隔离，以及基于 SimpleVectorStore 的向量存储与语义搜索。包含一个结合函数调用 + 对话记忆 + 流式返回的综合助手示例。
